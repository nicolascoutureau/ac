# Configuration for Cog ⚙️
# Reference: https://cog.run/yaml

build:
  # set to true if your model requires a GPU
  gpu: false

  # a list of ubuntu apt packages to install
  system_packages:
    - "ffmpeg"

  # python version in the form '3.11' or '3.11.4'
  python_version: "3.11"

  # path to a Python requirements.txt file
  python_requirements: requirements.txt

  # commands run after the environment is setup
  # Pre-download YOLOv8x model during build to avoid setup timeouts
  run:
    - "python -c 'from ultralytics import YOLO; print(\"Downloading YOLOv8x model...\"); model = YOLO(\"yolov8x.pt\"); print(\"Model downloaded successfully!\")'"

# predict.py defines how predictions are run on your model
predict: "predict.py:Predictor"
